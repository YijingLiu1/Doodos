const userNames = [];
const userImages = [];

let i;
for (i = 0; i < 5; i++) {
  const userName = userNames[Math.floor(Math.random() * userNames.length)];
  const userImage = userImages[Math.random() * userImage.length];
}
